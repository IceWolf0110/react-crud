spring:
  application:
    name: crud-backend

  datasource:
    url: jdbc:mariadb://localhost:3306/crud-backend
    username: root
    password: "01102000"
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

logging:
  level:
    org:
      springframework:
        security:
          config: error